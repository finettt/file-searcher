services:
  # 0. Qdrant vector database
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    networks:
      - app-network
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/6333'"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 15s

  # 1. OCR model (vision-capable, for scanned PDF fallback)
  llamacpp-ocr:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    ports:
      - "8003:8000"
    networks:
      - app-network
    deploy:
      resources:
        reservations:
          devices:
            - driver: cdi
              capabilities: [gpu]
              device_ids:
                - nvidia.com/gpu=all
        limits:
          memory: 8G
    volumes:
      - ./models:/models
    command: >
      --host 0.0.0.0 --port 8000
      -m /models/qwen3.5-4b-Q4_K_M.gguf
      --mmproj /models/mmproj-F16.gguf
      -c 8192 -ngl 99 -fa on -np 1 --cache-ram 0
      --image-min-tokens 1024 --image-max-tokens 2048 --reasoning off
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 90s

  # 2. Embedding model (waits for OCR to be healthy)
  llamacpp:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    ports:
      - "8000:8000"
    networks:
      - app-network
    deploy:
      resources:
        reservations:
          devices:
            - driver: cdi
              capabilities: [gpu]
              device_ids:
                - nvidia.com/gpu=all
        limits:
          memory: 8G
    volumes:
      - ./models:/models
    command: ["--host", "0.0.0.0", "--port", "8000", "-m", "/models/nomic-embed.gguf", "-ngl", "99", "--embedding", "--alias", "${EMBEDDING_MODEL}", "--ubatch-size", "2048", "--batch-size", "2048", "-np", "1"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s

  # 3. FileBrowser (file preview / download)
  filebrowser:
    image: filebrowser/filebrowser:latest
    networks:
      - app-network
    ports:
      - "8080:80"
    volumes:
      - ${DATA_DIR}:/srv:ro
      - ./filebrowser/filebrowser.db:/database.db
      - ./filebrowser/settings.json:/config/settings.json
    environment:
      - FB_NOAUTH=true
    restart: unless-stopped

  # 3a. Cross-encoder reranker (GPU, Qwen3-Reranker-0.6B)
  llamacpp-reranker:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    ports:
      - "8004:8000"
    networks:
      - app-network
    deploy:
      resources:
        reservations:
          devices:
            - driver: cdi
              capabilities: [gpu]
              device_ids:
                - nvidia.com/gpu=all
        limits:
          memory: 8G
    volumes:
      - ./models:/models
    command: >
      --host 0.0.0.0 --port 8000
      -m /models/Qwen3-Reranker-0.6B-Q4_K_M.gguf
      -ngl 99 --rerank
      --alias Qwen3-Reranker-0.6B
      --batch-size 2048 --ubatch-size 2048
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 60s

  # 4. Indexer service (owns search, rebuild, diff, file preview, progress)
  indexer:
    image: ghcr.io/finettt/file-searcher:stable
    container_name: indexer
    networks:
      - app-network
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL:-http://llamacpp:8000/v1}
      - EMBEDDING_MODEL=${EMBEDDING_MODEL:-text-embedding-3-small}
      - OCR_API_KEY=${OCR_API_KEY:-}
      - OCR_LLM_BASE_URL=${OCR_LLM_BASE_URL:-http://llamacpp-ocr:8000/v1}
      - OCR_LLM_MODEL=${OCR_LLM_MODEL:-qwen3.5-4b}
      - FILEBROWSER_URL=${FILEBROWSER_URL:-http://filebrowser:80}
      - QDRANT_URL=${QDRANT_URL:-http://qdrant:6333}
      - QDRANT_COLLECTION=${QDRANT_COLLECTION:-file_searcher}
      - RERANKER_BASE_URL=${RERANKER_BASE_URL:-http://llamacpp-reranker:8000/v1}
      - RERANKER_MODEL=${RERANKER_MODEL:-Qwen3-Reranker-0.6B}
      - HIDE_HEALTH=${HIDE_HEALTH:-false}
    volumes:
      - ${DATA_DIR}:/data:ro
    command: ["main_indexer.py", "/data", "--port", "8002", "--filebrowser-url", "${FILEBROWSER_URL:-http://filebrowser:80}"]
    deploy:
      resources:
        limits:
          memory: 2G
    restart: unless-stopped
    depends_on:
      llamacpp:
        condition: service_healthy
      qdrant:
        condition: service_healthy
      llamacpp-reranker:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/api/health"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s
    stop_grace_period: 120s

  # 5. Nginx reverse proxy (serves static frontend, proxies API/WS to indexer)
  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    image: ghcr.io/finettt/file-searcher-nginx:stable
    container_name: nginx
    ports:
      - "8001:8001"
    networks:
      - app-network
    deploy:
      resources:
        limits:
          memory: 128M
    restart: unless-stopped
    depends_on:
      indexer:
        condition: service_healthy

networks:
  app-network:
    driver: bridge

volumes:
  qdrant_data:
